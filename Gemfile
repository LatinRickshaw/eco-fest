source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# github-pages uses jekyll-3.9 / liquid-4.0 which rely on Object#tainted?,
# removed in Ruby 3.2. Use Ruby 3.1.x locally (see .ruby-version).
# GitHub Pages itself builds on Ruby 3.1 so production is fine either way.
